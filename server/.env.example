BYBIT_BASE_URL=https://api.bybit.com
PORT=3333
CORS_ORIGIN=http://localhost:5173

# SETTINGS_ENCRYPTION_KEY is generated automatically on first boot and
# appended below by the server itself — do not set it by hand.
# Your Bybit API key/secret are entered through the app's Settings page,
# not here — they're stored encrypted in server/data/app.db.
